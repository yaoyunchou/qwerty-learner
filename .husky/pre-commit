#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

cd "$(dirname "$0")/.." || exit 1

# 只处理「已暂存」文件：eslint + prettier 由 lint-staged 编排，避免对全仓库 / dist 跑 Prettier 导致崩溃或超长耗时
bash scripts/with-node-path.sh node ./node_modules/lint-staged/bin/lint-staged.js
