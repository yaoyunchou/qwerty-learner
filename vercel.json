{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "corepack enable && HUSKY=0 pnpm install --frozen-lockfile",
  "buildCommand": "pnpm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
